
const Button = ( {buttonName} ) => {
    return (
        <div>
            <button className="button_style">
                {buttonName}
            </button>
        </div>
    );
};

export default Button;